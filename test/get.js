var expect = require('expect.js');
var gard = require('../index');

require('harmony-reflect');

describe('Proxy.Get', function () {
    // it('should return a value if I have the read permission', function () {});
});
